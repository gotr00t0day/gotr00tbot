def convertmin(minutes):
    seconds = 60
    calc = minutes * seconds
    return calc


def converthr(hours):
    secondsperhour = 3600
    calc = hours * secondsperhour
    return calc


def convertdays(days):
    secondsperday = 86400
    calc = days * secondsperday
    return calc